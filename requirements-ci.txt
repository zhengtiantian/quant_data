# Minimal dependencies for pytest in CI — only what test modules actually import at module level.
# Heavy packages (lightgbm, mlflow, newspaper3k, crowd-kit, etc.) are excluded to keep CI fast.
pandas>=2.2.2
numpy>=1.26.4
pymongo>=4.7.0
python-dotenv>=1.0.1
pytest>=8.0.0
pytest-cov>=5.0.0
